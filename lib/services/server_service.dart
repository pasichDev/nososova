

class ServerService {
  /*final String baseUrl;

  ServerService(this.baseUrl);

  Future<List<ServerData>> fetchDataFromServer() async {
    final response = await http.get(Uri.parse('$baseUrl/data'));
    if (response.statusCode == 200) {
      // Розпарсіть відповідь сервера та поверніть дані
      final data = serverDataFromJson(response.body);
      return data;
    } else {
      throw Exception('Не вдалося отримати дані з сервера');
    }
  }

   */
}
